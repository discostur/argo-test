apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: flux-repo
  namespace: flux-system
spec:
  interval: 1m
  url: "https://github.com/discostur/argo-test.git"
  ref:
    branch: main
